@model ActualFileStorage.PL.Controllers.ShortenerController.SimpleViewModel
@{
    ViewBag.Title = "Unpack";
}

<h2>Received @Model.id</h2>
<h2>@Model.hello.Count()</h2>
@foreach (var d in Model.hello)
{
    <h2>@d.Id @d.Name @d.Visibility @d.CreationTime</h2>
}

